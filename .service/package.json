{
  "name": "game-of-prompts-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Game of Prompts Celaut microVM — full-surface MCP (Streamable HTTP) + REST API over the on-chain competition registry, with seed/unsigned signer.",
  "main": "server-http.mjs",
  "scripts": {
    "start": "node server-http.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@fleet-sdk/compiler": "^0.12.0",
    "@fleet-sdk/core": "^0.12.0",
    "@fleet-sdk/crypto": "^0.11.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "reputation-system": "github:reputation-systems/reputation-system"
  }
}
